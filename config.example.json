{
  "_说明": "任务配置模板。复制为 config.json 并按实际情况填写，config.json 已被 .gitignore 忽略。字段含义见 docs/cli-protocol.md。",

  "execPath": "C:/Program Files/Google/Chrome/Application/chrome.exe",
  "userDataDir": "请填写已登录抖音的 Chrome 用户数据目录",
  "taskId": "1",
  "douyinId": "1",
  "videoPath": "请填写待发布视频的绝对路径",
  "_cartUrl说明": "留空表示发布不带商品的纯内容视频，挂车步骤会自动跳过。",
  "cartUrl": "请填写商品链接，或留空发纯内容视频",

  "title": "视频标题",
  "desc": "标签一,标签二,标签三",
  "cartTitel": "",

  "publishTimeMode": "立即发布",
  "publishTime": "24",
  "whoCanSee": "仅自己可见",
  "savePermission": "不允许",
  "selfDeclaration": "无需添加自主声明",

  "headless": false,

  "_运行时选项": "以下四项全部可选，删掉它们行为与默认完全一致。详见 docs/cli-protocol.md 第 3.3~3.6 节。",

  "_timeouts说明": "各环节等待上限（秒）。只写想改的那几项即可，未写的保持默认。最常需要调整的是 upload——视频大或上行带宽窄时调大。",
  "timeouts": {
    "total": 600,
    "upload": 300,
    "publish": 120
  },

  "_browserArgs说明": "追加的 Chrome 启动参数，拼在内置参数之后。--user-data-dir 与 --remote-debugging-* 会被拒绝（它们由程序自身管理）。",
  "browserArgs": [],

  "_logLevel说明": "quiet（仅警告）/ normal（默认）/ debug（含元素查找细节）。批量调度时用 quiet 可大幅压低日志量。",
  "logLevel": "normal",

  "_skip说明": "要跳过的阶段。可跳过：title、setting、declaration、cart、cover。upload 与 publish 等必要环节不可跳过。发布纯内容视频只需把 cartUrl 留空，无需在这里填 cart。",
  "skip": [],

  "_screenshot说明": "失败时自动截取页面现场，默认开启。dir 留空则存到系统临时目录。截图路径会出现在结果 JSON 的 screenshot 字段中。注意截图可能含账号信息。",
  "screenshot": {
    "onFailure": true,
    "dir": ""
  },

  "_progress说明": "进度回报，默认关闭。开启后 stdout 会变成多行 JSON（每行一个，结果永远在最后一行），若上游是整段解析 stdout 则不要开启。heartbeat 是长等待期间的心跳间隔（秒），用来区分「正在上传」和「已经卡死」，填 0 则只报步骤不发心跳。",
  "progress": {
    "enabled": false,
    "heartbeat": 15
  }
}
